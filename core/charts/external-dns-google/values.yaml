---
external-dns:
  image:
    registry: registry.k8s.io
    repository: external-dns/external-dns
    tag: v0.13.6

  serviceAccount:
    create: false
    name: external-dns

  sources:
    - service
    - ingress

  interval: 5m

  logLevel: error

  domainFilters: # FIXME: domains
    - home.muehlbachler.io
  annotationFilter: external-dns.alpha.kubernetes.io/provider=public

  txtOwnerId: external-dns-public-cluster-dev-google # FIXME: naming
  provider: google
  google:
    project: tuxnet-385112 # FIXME: project

  resources:
    requests:
      cpu: 5m
      memory: 64Mi
    limits:
      cpu: 10m
      memory: 80Mi
