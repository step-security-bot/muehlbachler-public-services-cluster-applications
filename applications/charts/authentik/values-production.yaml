---
domain: &domain sso.home.muehlbachler.io # FIXME: domain

ldap:
  domain: *domain

authentik:
  ingress:
    hosts:
      - host: *domain
        paths:
          - path: "/"
            pathType: Prefix
    tls:
      - secretName: authentik-tls-cert
        hosts:
          - *domain
